<%= form_with(model: setlist_item) do |form| %>
  <% if setlist_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(setlist_item.errors.count, "error") %> prohibited this setlist_item from being saved:</h2>

      <ul>
        <% setlist_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :setlist_id, style: "display: block" %>
    <%= form.text_field :setlist_id %>
  </div>

  <div>
    <%= form.label :song_id, style: "display: block" %>
    <%= form.text_field :song_id %>
  </div>

  <div>
    <%= form.label :position, style: "display: block" %>
    <%= form.number_field :position %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
