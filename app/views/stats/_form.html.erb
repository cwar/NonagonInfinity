<%= form_with(model: stat) do |form| %>
  <% if stat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(stat.errors.count, "error") %> prohibited this stat from being saved:</h2>

      <ul>
        <% stat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :shows_attended, style: "display: block" %>
    <%= form.number_field :shows_attended %>
  </div>

  <div>
    <%= form.label :favorite_songs, style: "display: block" %>
    <%= form.text_area :favorite_songs %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
