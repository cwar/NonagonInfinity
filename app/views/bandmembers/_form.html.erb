<%= form_with(model: bandmember) do |form| %>
  <% if bandmember.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bandmember.errors.count, "error") %> prohibited this bandmember from being saved:</h2>

      <ul>
        <% bandmember.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :artist_id, style: "display: block" %>
    <%= form.text_field :artist_id %>
  </div>

  <div>
    <%= form.label :people_id, style: "display: block" %>
    <%= form.text_field :people_id %>
  </div>

  <div>
    <%= form.label :role, style: "display: block" %>
    <%= form.text_field :role %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
